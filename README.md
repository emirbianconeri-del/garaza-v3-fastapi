# Garaza v3 (FastAPI)

REST API za upravljanje vozilima u garaži (CRUD) + API testovi.

## Tech stack
- Python
- FastAPI
- SQLite
- SQLAlchemy
- Pytest

## Pokretanje (lokalno)

```bash
venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
